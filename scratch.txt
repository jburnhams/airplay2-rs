So we can add `Retransmit(u16, u16)` to `StreamerCommand`.
And add a method `pub async fn retransmit(&self, seq_start: u16, count: u16)` to `PcmStreamer` which sends `Retransmit(seq_start, count)` to `cmd_tx`.
Inside the `stream` loop, when `cmd_rx.try_recv()` gives `Retransmit(seq_start, count)`, we get the buffered packets and send them via `ConnectionManager::send_rtcp_control`!
Wait, `PcmStreamer` only has `self.connection` which is `Arc<dyn RtpSender>`.
So `RtpSender` needs a method `async fn send_rtcp_control(&self, packet: &[u8]) -> Result<(), AirPlayError>`.
And `ConnectionManager` implements it and sends to `self.sockets.control`.

Wait, but we need to receive `RetransmitRequest` from the device.
Who receives from `self.sockets.control`?
We can spawn a task inside `ConnectionManager::setup_session` (after `self.sockets` is populated).
Because `sockets` is a `Mutex<Option<UdpSockets>>`, we shouldn't keep it locked.
We can clone `UdpSockets.control`?
Yes, `UdpSocket` has `try_clone()` (which creates a new socket referring to the same fd) or we can use `Arc<UdpSocket>`.
Using `Arc<UdpSocket>` inside `UdpSockets` is better.
Let's see: `src/connection/manager.rs` has `pub(crate) struct UdpSockets`.
