--- integration_tests/tests/heartbeat_integration.rs
+++ integration_tests/tests/heartbeat_integration.rs
@@ -49,8 +49,9 @@
                             // Update our initial last seen to the current one
                             initial_last_seen = Some(current);
                         }
-                    } else if initial_last_seen.is_none() {
-                        initial_last_seen = device.last_seen;
-                        discovered = true;
                     }
+                    if initial_last_seen.is_none() {
+                        initial_last_seen = device.last_seen;
+                        discovered = true;
+                    }
                 }
             }
